spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/melancholy}
    username: ${DB_USERNAME:melancholy}
    password: ${DB_PASSWORD:melancholy}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
